---
phase: 05-can-you-fix-the-ui-it-has-a-harsh-and-very-dark-theme-can-yo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/App.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-05
must_haves:
  truths:
    - User sees a visibly lighter and calmer app shell and control area.
    - User sees one consistent palette applied across page shell, header text, search, and controls.
    - User sees coherent theme behavior in default and dark-preference environments.
  artifacts:
    - path: src/index.css
      provides: Softened global semantic tokens and controlled color-scheme behavior.
    - path: src/App.css
      provides: App shell, search, control, and status styling wired to semantic tokens.
  key_links:
    - from: src/index.css
      to: src/App.css
      via: CSS custom properties consumed by component classes.
      pattern: var\(--
    - from: src/App.jsx
      to: src/App.css
      via: Existing className usage with unchanged JSX behavior.
      pattern: className=
---

<objective>
Soften global and app-shell UI tone using token-driven theming without changing behavior.

Purpose: Remove harsh dark/contrasty styling drift and establish one maintainable palette layer.
Output: Updated global tokens and App-level styles that are visually calmer and consistent.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-can-you-fix-the-ui-it-has-a-harsh-and-very-dark-theme-can-yo/05-RESEARCH.md
@src/index.css
@src/App.css
@src/App.jsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define softened semantic theme tokens in global stylesheet</name>
  <files>src/index.css</files>
  <action>Refine root-level color tokens into a light-first softened palette (surfaces, text tiers, borders, accents, status, shadows) and rebalance prefers-color-scheme: dark values so dark mode stays readable without near-black harshness, per UI-01 and UI-05. Keep token names stable or migrate carefully with direct replacements so no stale references remain.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Global token layer represents a calmer theme and compiles cleanly in both default and dark-preference CSS branches.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App shell and controls to consume semantic tokens only</name>
  <files>src/App.css</files>
  <action>Replace hardcoded App-level colors for header, search, controls, hover, and status pills with semantic token references from src/index.css (UI-02). Preserve current spacing/layout and do not alter JSX structure or any event/data logic (UI-06 guardrail).</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>App-level styles are token-driven, visually softened, and behavior remains unchanged.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                      |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| UI theme tokens -> user interpretation | Visual state cues cross from implementation to user understanding and can be misleading if contrast is too weak. |

## STRIDE Threat Register

| Threat ID | Category | Component                             | Disposition | Mitigation Plan                                                                                                                      |
| --------- | -------- | ------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-01   | S        | src/App.css status/loading indicators | mitigate    | Keep distinct status token pairs for loading/error with readable foreground/background contrast and preserve alert semantics in JSX. |
| T-05-02   | T        | src/index.css shared token layer      | mitigate    | Centralize colors into semantic tokens and remove conflicting literals to reduce accidental style tampering/drift across components. |
| T-05-03   | R        | Visual regression accountability      | accept      | This phase is styling-only with no business-state mutation; rely on lint/build plus follow-up verification in plan 02.               |
| T-05-04   | D        | UI readability under dark preference  | mitigate    | Rebalance dark-mode tokens away from near-black backgrounds and validate compiled output in both schemes.                            |

</threat_model>

<verification>
- npm run lint
- npm run build
</verification>

<success_criteria>

- UI-01: App shell and controls present a visibly softer theme.
- UI-02: App-level palette uses centralized semantic tokens.
- UI-05: Theme remains coherent in default and dark-preference rendering.
  </success_criteria>

<output>
After completion, create .planning/phases/05-can-you-fix-the-ui-it-has-a-harsh-and-very-dark-theme-can-yo/05-01-SUMMARY.md
</output>
