---
phase: 03-unified-table-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/users-table/model/filterRows.js
  - src/features/users-table/model/filterRows.test.js
  - package.json
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - Rows can be filtered by username or email text query.
    - Filtering is case-insensitive and deterministic.
  artifacts:
    - path: src/features/users-table/model/filterRows.js
      provides: Pure row filtering utility.
    - path: src/features/users-table/model/filterRows.test.js
      provides: Automated filter behavior tests.
  key_links:
    - from: src/App.jsx
      to: src/features/users-table/model/filterRows.js
      via: Query-driven filtered rows memo.
      pattern: filterRows
---

<objective>
Build tested filtering core for username/email search.

Purpose: Isolate UX-03 behavior in pure function before UI wiring.
Output: Filter utility, tests, and npm script.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.jsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for username/email filtering</name>
  <files>src/features/users-table/model/filterRows.test.js, package.json</files>
  <behavior>
    - Empty query returns full row list.
    - Query matches username (case-insensitive).
    - Query matches email (case-insensitive).
    - No matches returns empty list.
  </behavior>
  <action>Create node:test suite for filterRows and add npm script test:phase3-filter.</action>
  <verify>
    <automated>npm run test:phase3-filter</automated>
  </verify>
  <done>Tests define expected UX-03 filtering behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement filterRows utility</name>
  <files>src/features/users-table/model/filterRows.js</files>
  <behavior>
    - Uses query against username/email only.
    - Handles null/undefined values safely.
  </behavior>
  <action>Implement pure filterRows(rows, query) utility that trims query and applies case-insensitive matching.</action>
  <verify>
    <automated>npm run test:phase3-filter</automated>
  </verify>
  <done>Filter tests pass and utility ready for UI integration.</done>
</task>
</tasks>

<verification>
- npm run test:phase3-filter
- npm run lint
</verification>

<output>
After completion, create .planning/phases/03-unified-table-experience/03-01-SUMMARY.md
</output>
