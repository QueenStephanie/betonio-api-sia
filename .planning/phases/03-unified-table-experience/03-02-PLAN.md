---
phase: 03-unified-table-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/App.jsx
  - src/App.css
  - src/features/users-table/components/UsersTable.jsx
  - src/features/users-table/components/UsersTable.css
autonomous: true
requirements:
  - DATA-02
  - UX-03
must_haves:
  truths:
    - User sees one merged table with required columns.
    - User can search username/email and results update immediately.
    - Personal row remains first when included in filtered results.
  artifacts:
    - path: src/App.jsx
      provides: Search input and filtered rows wiring.
    - path: src/features/users-table/components/UsersTable.jsx
      provides: Empty-state feedback when filter yields no rows.
  key_links:
    - from: src/App.jsx
      to: src/features/users-table/model/filterRows.js
      via: useMemo filtering pipeline.
      pattern: useMemo
---

<objective>
Deliver final Phase 3 unified table exploration UX.

Purpose: Complete required table experience and search/filter interaction.
Output: Search UI, filtered rendering, and no-results feedback.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.jsx
@src/features/users-table/components/UsersTable.jsx
@src/features/users-table/model/rowContract.js
@.planning/phases/03-unified-table-experience/03-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add query UI and filtered rows wiring</name>
  <files>src/App.jsx, src/App.css</files>
  <action>Add search input bound to query state, derive filtered rows using filterRows, and display helper text with match count. Keep existing refresh/loading/error controls functional.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Typing username/email query updates rendered rows immediately.</done>
</task>
<task type="auto">
  <name>Task 2: Add no-results table feedback</name>
  <files>src/features/users-table/components/UsersTable.jsx, src/features/users-table/components/UsersTable.css</files>
  <action>Render a semantic empty-result table row when rows list is empty so users see explicit filter result feedback.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Users see clear no-results state without layout break.</done>
</task>
</tasks>

<verification>
- npm run test:rows
- npm run test:phase2-api
- npm run test:phase3-filter
- npm run lint
- npm run build
</verification>

<output>
After completion, create .planning/phases/03-unified-table-experience/03-02-SUMMARY.md
</output>
