---
phase: 02-api-fetch-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/features/users-table/hooks/useUsersTableData.js
  - src/features/users-table/components/UsersTable.css
  - src/App.jsx
  - src/App.css
autonomous: true
requirements:
  - DATA-05
  - UX-02
must_haves:
  truths:
    - User sees loading state while request is in progress.
    - User sees error state on request failure.
    - User can refresh to re-request data.
  artifacts:
    - path: src/features/users-table/hooks/useUsersTableData.js
      provides: Fetch lifecycle state and refresh handler.
    - path: src/App.jsx
      provides: Loading, error, and refresh UI wiring.
  key_links:
    - from: src/features/users-table/hooks/useUsersTableData.js
      to: src/features/users-table/api/usersApi.js
      via: fetchApiUsers call
      pattern: fetchApiUsers
---

<objective>
Wire fetch lifecycle states into table UI.

Purpose: Deliver loading/error/refresh user behaviors.
Output: Hook-driven data flow and app controls.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.jsx
@src/features/users-table/components/UsersTable.jsx
@src/features/users-table/model/mergeRows.js
@src/features/users-table/model/rowModel.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement fetch lifecycle hook</name>
  <files>src/features/users-table/hooks/useUsersTableData.js</files>
  <action>Create a hook that loads remote users on mount, maps them with normalizeApiUser, merges with personal row, exposes loading/error state, and provides refreshData action with abort-safe request handling.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Hook exposes { rows, isLoading, errorMessage, refreshData } with stable behavior.</done>
</task>
<task type="auto">
  <name>Task 2: Wire hook into App with refresh and state messaging</name>
  <files>src/App.jsx, src/App.css, src/features/users-table/components/UsersTable.css</files>
  <action>Replace fixture rows with hook data, add refresh button, show loading text while requesting, and show retry-friendly error banner when request fails while keeping table visible with personal row baseline.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>User can refresh API data and sees clear loading/error state transitions.</done>
</task>
</tasks>

<verification>
- npm run test:rows
- npm run test:phase2-api
- npm run lint
- npm run build
</verification>

<output>
After completion, create .planning/phases/02-api-fetch-lifecycle/02-02-SUMMARY.md
</output>
