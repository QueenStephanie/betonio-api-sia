---
phase: 01-data-shape-row-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/users-table/model/rowContract.js
  - src/features/users-table/model/rowModel.js
  - src/features/users-table/model/rowModel.test.js
  - package.json
autonomous: true
requirements:
  - DATA-04
  - UX-04
must_haves:
  truths:
    - API user full names are split into first and last name fields before rendering.
    - Rows expose a deterministic missing-field indicator for key fields.
  artifacts:
    - path: src/features/users-table/model/rowContract.js
      provides: Shared row constants and required key-field contract.
    - path: src/features/users-table/model/rowModel.js
      provides: Pure row normalization helpers for API and personal rows.
    - path: src/features/users-table/model/rowModel.test.js
      provides: Automated checks for split-name and missing-field behavior.
  key_links:
    - from: src/features/users-table/model/rowModel.js
      to: src/features/users-table/model/rowContract.js
      via: Imported row constants and required-field keys.
      pattern: import.*rowContract
    - from: src/features/users-table/model/rowModel.test.js
      to: src/features/users-table/model/rowModel.js
      via: Unit tests for normalizeApiUser/createMissingFieldMap logic.
      pattern: node:test assertions
---

<objective>
Lock the row data contract and normalization logic so all downstream UI work receives one stable table-row shape.

Purpose: Prevent schema drift and fragile JSX parsing by handling transforms in pure utilities.
Output: Row contract file, normalization helpers, and passing model-level tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@src/App.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define row contracts and failing behavior tests</name>
  <files>src/features/users-table/model/rowContract.js, src/features/users-table/model/rowModel.test.js, package.json</files>
  <behavior>
    - Test 1: API name "Jane Smith" maps to firstName="Jane" and lastName="Smith" (per DATA-04).
    - Test 2: Single-token API name "Plato" maps to firstName="Plato" and lastName="".
    - Test 3: Missing email/zipcode marks those fields in missingFields indicator (per UX-04).
  </behavior>
  <action>Create row contract exports for required table keys and source labels, add a node:test suite that encodes expected transformations, and add a package script for running these unit tests. Keep tests red initially to enforce TDD.</action>
  <verify>
    <automated>npm run test:rows</automated>
  </verify>
  <done>Test file exists with coverage for split-name and missing-field rules and fails before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement row normalization helpers to satisfy tests</name>
  <files>src/features/users-table/model/rowModel.js, src/features/users-table/model/rowModel.test.js</files>
  <behavior>
    - Test 1: normalizeApiUser returns canonical row keys: id, firstName, lastName, username, email, zipcode, source, missingFields.
    - Test 2: createMissingFieldMap flags empty/null key fields as true and populated fields as false.
    - Test 3: splitName trims whitespace and preserves multi-part last names.
  </behavior>
  <action>Implement splitName, createMissingFieldMap, normalizeApiUser, and createPersonalRow in rowModel.js using the row contract constants. Keep logic pure and deterministic so UI rendering stays presentation-only.</action>
  <verify>
    <automated>npm run test:rows</automated>
  </verify>
  <done>All row-model tests pass and helpers produce canonical rows that include source and missing-field metadata.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                |
| ----------------------------------------- | -------------------------------------------------------------------------- |
| External API payload -> row normalization | Untrusted remote values are transformed into renderable row fields.        |
| Internal row model -> table UI            | Incorrect row shape could break rendering or hide missing data indicators. |

## STRIDE Threat Register

| Threat ID | Category | Component                                  | Disposition | Mitigation Plan                                                                                                            |
| --------- | -------- | ------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-01-01   | T        | src/features/users-table/model/rowModel.js | mitigate    | Normalize all input through explicit key mapping; never pass raw nested API objects to UI.                                 |
| T-01-02   | I        | missing-field indicator pipeline           | mitigate    | Derive missingFields for key columns and render explicit indicator for absent values.                                      |
| T-01-03   | D        | splitName utility                          | accept      | Extremely long or malformed names are low-risk in this educational dataset; handle safely via trim/split without throwing. |

</threat_model>

<verification>
Run row model tests and lint:
- npm run test:rows
- npm run lint
</verification>

<success_criteria>

- API full-name values are always split into first/last fields (per DATA-04).
- Row objects expose deterministic missing-field metadata used for UX indicators (per UX-04).
- Tests and lint pass without disabling lint rules.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-data-shape-row-integrity/01-01-SUMMARY.md`
</output>
