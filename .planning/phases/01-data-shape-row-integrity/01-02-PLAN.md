---
phase: 01-data-shape-row-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/users-table/model/mergeRows.js
  - src/features/users-table/model/mergeRows.test.js
  - src/features/users-table/components/UsersTable.jsx
  - src/features/users-table/components/UsersTable.css
  - src/App.jsx
  - src/App.css
  - package.json
autonomous: true
requirements:
  - DATA-03
  - UX-01
must_haves:
  truths:
    - Personal row is rendered first in the unified table output.
    - Every row shows a source marker identifying Personal or API origin.
  artifacts:
    - path: src/features/users-table/model/mergeRows.js
      provides: Deterministic merge ordering for personal-first row output.
    - path: src/features/users-table/components/UsersTable.jsx
      provides: Semantic table rendering with source and missing-data indicators.
    - path: src/App.jsx
      provides: App wiring that feeds personal row plus API-like rows through the merge pipeline.
  key_links:
    - from: src/App.jsx
      to: src/features/users-table/model/mergeRows.js
      via: Merge call that prepends personal row before API rows.
      pattern: mergeRows\(
    - from: src/features/users-table/components/UsersTable.jsx
      to: src/features/users-table/model/rowContract.js
      via: Header and source token usage based on canonical row keys.
      pattern: import.*rowContract
---

<objective>
Deliver the user-visible Phase 1 table experience where row origin and data quality are obvious and personal data is always first.

Purpose: Satisfy assignment-visible row-order and provenance requirements before API lifecycle work begins.
Output: Personal-first merge helper, rendered table component, and app-level wiring with deterministic sample API rows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-shape-row-integrity/01-01-SUMMARY.md
@src/App.jsx
@src/App.css

<interfaces>
From src/App.jsx:
```jsx
function App() {
  return (...) 
}

export default App

````

Expected from Plan 01:
```js
export function createPersonalRow(personalInput)
export function normalizeApiUser(apiUser)
export const ROW_SOURCE
````

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add merge-order behavior tests and implementation</name>
  <files>src/features/users-table/model/mergeRows.js, src/features/users-table/model/mergeRows.test.js, package.json</files>
  <behavior>
    - Test 1: mergeRows(personalRow, apiRows) always places personal row at index 0 (per DATA-03).
    - Test 2: API rows retain original relative order after the personal row prepend.
    - Test 3: Merge result preserves source labels so Personal/API remains identifiable (per UX-01).
  </behavior>
  <action>Create mergeRows helper with node:test coverage, and add/update package scripts so row-model and merge tests can be executed together.</action>
  <verify>
    <automated>npm run test:rows</automated>
  </verify>
  <done>Merge helper and tests guarantee personal-first ordering with stable API row order.</done>
</task>

<task type="auto">
  <name>Task 2: Build table component with source and missing-data indicators</name>
  <files>src/features/users-table/components/UsersTable.jsx, src/features/users-table/components/UsersTable.css, src/App.css</files>
  <action>Create a semantic table component that renders columns ID, First Name, Last Name, Username, Email, Zipcode, and row source. Render a clear visual indicator for missing key fields using missingFields metadata from Plan 01. Keep styling scoped and readable on desktop/mobile widths.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Table renders canonical rows with visible source labels and missing-field indicators while meeting assignment column naming.</done>
</task>

<task type="auto">
  <name>Task 3: Replace starter screen with personal-first table wiring</name>
  <files>src/App.jsx</files>
  <action>Replace starter content with users-table feature wiring that creates one personal row, transforms a small API-like fixture through normalizeApiUser, merges with mergeRows, and renders UsersTable. Keep this data local in Phase 1; live fetching is explicitly deferred to Phase 2.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Application shows unified table with personal row first, followed by transformed API fixture rows and source/missing indicators (per DATA-03 and UX-01).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                |
| ------------------------------------ | ------------------------------------------------------------------------------------------ |
| Normalized row model -> UI rendering | UI must not assume fields exist without using missing-field metadata.                      |
| Local fixture data -> merge pipeline | Even local/demo inputs are treated as potentially incomplete and must be normalized first. |

## STRIDE Threat Register

| Threat ID | Category | Component                                          | Disposition | Mitigation Plan                                                                                                 |
| --------- | -------- | -------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-01-04   | T        | src/features/users-table/model/mergeRows.js        | mitigate    | Enforce mergeRows contract with tests proving personal row remains first and source labels survive merge.       |
| T-01-05   | I        | src/features/users-table/components/UsersTable.jsx | mitigate    | Render explicit source tokens and missing-data markers to prevent ambiguous row provenance or silent blanks.    |
| T-01-06   | E        | src/App.jsx feature wiring                         | accept      | App is client-only and has no privilege boundary; risk of privilege escalation is not applicable in this phase. |

</threat_model>

<verification>
- npm run test:rows
- npm run lint
- npm run build
</verification>

<success_criteria>

- Personal row is always first in merged table output (per DATA-03).
- Users can identify each row source as Personal or API (per UX-01).
- Table remains render-safe even when key fields are missing.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-data-shape-row-integrity/01-02-SUMMARY.md`
</output>
