---
phase: 04-submission-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/phases/04-submission-delivery/04-PUBLISH-EVIDENCE.md
autonomous: true
requirements:
  - DLV-01
must_haves:
  truths:
    - Student has a publicly accessible GitHub repository for this project.
    - Repository default branch includes the verified working project state.
    - Student can retrieve the canonical repository landing URL.
  artifacts:
    - path: README.md
      provides: Assignment-facing repository guidance and canonical URL placement.
    - path: .planning/phases/04-submission-delivery/04-PUBLISH-EVIDENCE.md
      provides: Captured quality-gate outcomes and final canonical URL evidence.
  key_links:
    - from: local git repository
      to: public GitHub repository
      via: git remote add/set-url origin + git push default branch
      pattern: git push
    - from: package.json scripts
      to: publish evidence
      via: lint/build/test command outputs recorded before publish
      pattern: npm run lint && npm run build
---

<objective>
Publish the canonical repository in a verifiably ready state.

Purpose: Satisfy DLV-01 with a public GitHub repository URL that points to a quality-checked project snapshot.
Output: Published repository plus evidence file containing quality-gate results and canonical URL.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-submission-delivery/04-CONTEXT.md
@package.json
@README.md
@.planning/phases/03-unified-table-experience/03-02-SUMMARY.md
@.planning/phases/05-can-you-fix-the-ui-it-has-a-harsh-and-very-dark-theme-can-yo/05-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Establish submission-facing repository documentation and evidence scaffold</name>
  <files>README.md, .planning/phases/04-submission-delivery/04-PUBLISH-EVIDENCE.md</files>
  <action>Replace starter-template README content with assignment-facing instructions and add a dedicated "Submission Repository URL" section (per D-02). Create 04-PUBLISH-EVIDENCE.md with placeholders for command results, remote details, and canonical URL so publish proof is captured in-repo.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('README.md','utf8');if(!r.includes('Submission Repository URL')){process.exit(1)}"</automated>
  </verify>
  <done>README is assignment-oriented and evidence scaffold exists with canonical URL field.</done>
</task>

<task type="auto">
  <name>Task 2: Run pre-publish quality gates and capture outputs</name>
  <files>.planning/phases/04-submission-delivery/04-PUBLISH-EVIDENCE.md</files>
  <action>Execute `npm run lint`, `npm run build`, and all existing phase test commands before any publish action (per D-03 and D-04). Record pass/fail output summaries and timestamps in 04-PUBLISH-EVIDENCE.md. If any command fails, stop and remediate before proceeding.</action>
  <verify>
    <automated>npm run test:rows && npm run test:phase2-api && npm run test:phase3-filter && npm run lint && npm run build</automated>
  </verify>
  <done>Evidence file contains successful command results proving release readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Publish canonical GitHub repository and record URL</name>
  <files>.planning/phases/04-submission-delivery/04-PUBLISH-EVIDENCE.md, README.md</files>
  <action>Publish current repository to public GitHub on default branch (per D-01). Capture and record the canonical landing URL `https://github.com/&lt;owner&gt;/&lt;repo&gt;` (per D-02) in 04-PUBLISH-EVIDENCE.md and mirror that exact URL in README submission section.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const txt=fs.readFileSync('.planning/phases/04-submission-delivery/04-PUBLISH-EVIDENCE.md','utf8');if(!/https:\/\/github\.com\/[^\s/]+\/[^\s/]+/.test(txt)){process.exit(1)}"</automated>
  </verify>
  <done>Public repo URL is recorded once as canonical source and matches README submission URL.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                            |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| local workspace -> GitHub remote        | Code and metadata leave local trusted environment and become publicly accessible.                                      |
| command output -> publish evidence file | Verification claims are transcribed into persistent artifacts and can be misreported if not tied to executed commands. |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                                                                       |
| --------- | -------- | -------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | T        | git remote config    | mitigate    | Validate `origin` points to intended owner/repo before push and store remote URL in 04-PUBLISH-EVIDENCE.md.           |
| T-04-02   | I        | repository contents  | mitigate    | Run lint/build/tests before publish and ensure no secrets are introduced; publish only assignment artifacts per D-06. |
| T-04-03   | R        | publish evidence     | mitigate    | Record exact commands and timestamps in 04-PUBLISH-EVIDENCE.md so readiness claims are auditable.                     |
| T-04-04   | D        | submission readiness | mitigate    | Block publish on any failed quality gate to avoid shipping non-functional state.                                      |

</threat_model>

<verification>
- npm run test:rows
- npm run test:phase2-api
- npm run test:phase3-filter
- npm run lint
- npm run build
- Confirm canonical URL format: `https://github.com/<owner>/<repo>`
</verification>

<success_criteria>

- DLV-01 satisfied: repository is public and reachable at canonical landing URL.
- Canonical URL is present and consistent in README and 04-PUBLISH-EVIDENCE.md.
- All quality gates pass before URL handoff.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-submission-delivery/04-01-SUMMARY.md`
</output>
